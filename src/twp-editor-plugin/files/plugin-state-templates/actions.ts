export enum <%= classify(name) %>ActionTypes {
}

/**
 * Placeholder action type, replace with your real one
 */
export interface Some<%= classify(name) %>Action {
}

export type <%= classify(name) %>Action = Some<%= classify(name) %>Action;
